# 청산 트랜잭션(GX) · 셀프 그라인딩 · 티끌 절단 규명

> (원본: 메모리 goldminer-exit-txn, goldminer-grind-chunk)

## 청산 트랜잭션 — "한 번에 한 기계" (7/25 새 창)
- 발의(사장님): 상태체크 → 동시 청산 집행 → 고아 점검 → 고아만 정리 → 상태 재체크(그 사이 나머지 판단 보류) → 다음 대상. "어떤 녀석이 먼저 골 하면 나머지는 기다려야지". 타임아웃 10초, 고아 재청산 상한 2회. 해결사 = 언제나 우선(진입 계열 보류 포함, 보류=유예·재평가).
- 변경표 E1~E5: E1 g_exitTxn 원장 / E2 각 발사부 락 점검 / E3 발사 직전 전 다리 생존+surplus 라이브 재검(A안 흡수) / E4 사후 감사·그 건 고아만 재청산(상한 2회) / E5 타임아웃 10초 해제(DM_FLUSH 교착 교훈 — 교착 시 기본값=해제). 서버 측 기계(통문TP·BKUP·고아TP)는 락 불가 → 사전검사+사후감사 2중 흡수.
- 무변 확약: §0 약수익 헌법·§6 최고참 순서·연료 축·각 기계 대상 선정.
- 구현 완료(7/25 "진행해줘"): V1~V10+G1~G3 봉합, 배선 47곳, 3형제 동일. B2 상수(0.70/0.70/0.50) 3형제 반영. F7 에러 0(7/25). 라이브 미적용.
- ⚠ 상태(7/27 원장 오독 방지): A(연료 소실)는 E3+claim 예약이 구조 흡수(라이브 실증 미완). 실제 잔여 미결 = D(SNAP+ 5분 무한 리필+정합 이탈, 미규명·재료 대기)·C(BKUP 60초 스팸, D 파생 가능성).

## 청산 균형 조문 (7/27, 월요일 갭 사고 후속)
- 사장님 발의: ①단독 청산 금지 — 연료(수익 다리) 실현은 리스크 다리 동반 제거와 한 트랜잭션일 때만 ②몰림 방지 — 바이·셀 함께 덜어 발사 후 한 방향 쏠림 거부(이번: 바이 전체 청산→셀만 잔존→리스크 급증) ③락 중 발사 = [연료+리스크 동반] 묶음만 조건부 허용.
- **부호 반전 하드룰 → 철회(7/27)**: "부호는 뒤집힐 수 있다, 시소(±50% OK, 덩그러니만 이슈)가 깨지면 안 된다. 분할하더라도 한쪽 다 날리지 마라".
- **G0 v2 시소 보존 게이트(확정)**: ①사이드 소진 상한 = 제거랏 ≤ 사이드 잔존랏 × GX_WIPE_MAX **0.33**(VT 94% 제거 거부·29418266 34%·33753512 16% 통과) ②시소 커버리지 하한 = 상쇄측/위험측 ≥ C_min(회랑 effFloor 산술 재사용, 모집단 [111÷셀]→[전 매직 × 전 청산기계 × EXITTXN 단일 관문]) ③락·갭 창 강화 = C_min 최고단 **0.40** 강제 + 연료 단독 금지 예외 없음.
- **GX 구현 완료(7/27, 3빌드)**: P-A~P-I 13패치. G0(GX_SeesawGate — ExitTxn_Begin 관문, 소진 33%는 미동반 단독형에만·동반형은 커버리지 담당=FIRE 전량소각 설계 보존) / G1(락 단독 거부+C_min 0.40·SOLVER 화이트) / G2(GX_SessionOpen 60s 캐시, 무브로커 통과 안전핀) / G3(terminal 거절→청산 60s 쿨다운) / G4(RegisterIsland 서두 파기 — XGrind_Disarm이 먼저) / G5(섬 TP 소거 검증 3회+LOCKED 60s 재소거). 검증 앵커 13/13·괄호 무변·시뮬 11/11.
- 자기감사 2차: 🔴 GRIND_CLOSE_RETRY 백오프 미배선 3곳(PAIR·COMBO연료·FIRE연료)→P-J1~J3 봉합. 🟡 OverCap_Grind_Try 미게이트→P-K. 🟢 대시보드 GX 가드 슬롯 부재(컨펌 대기).
- **v5.5.4 정본 승격(7/27)**: GoldMiner_v5_5_4_{internal,locked}.mq5(#property "5.54") + TEST_A(5.5.4A-TEST). 정본 = v5.5.3 버그수리 전탑재 + GX 17패치(G0~G5+P-J·P-K). TEST_A만 GL-A 추가. 잔여: 사장님 F7 → 백테스트(GL off, 5월 씨앗+갭 주간) → internal 라이브 승격 + init 빌드 확인(L12). 미실행 백로그: P7(WLU_Tail 클레임 가드) internal·locked 이식 · GX 대시보드 슬롯.

## ★ 라이브 그라인딩 과다·티끌 절단 규명 (7/31)
- 발단: 라이브에서 그라인딩 과다 발동 — "자본금이 리스크 구간도 아니고, 그냥 청산 가능한 때 짧게 그라인딩으로 깎아 먹는다 / 이것도 기다려서 청산 보내면 되는데 왜 그라인딩하냐".
- 재료: 600029(Orbis USC, base×18, 헷징 ON) · 33753512(PUPrime USC, base×1, 헷징 미발동) · v5_5_3_internal(BUILD 20260717=7/23 정본, PM_OrphanSweep·ExitTxn·GX 없음 = 라이브 3계좌 이 빌드 추정).
- **코드 확정 원인**:
  - XGRIND_MAX_CHUNK **0.01** / _DEEP 0.02 / OVERCAP_CHUNK 0.05 = **절대 랏 상수, base 비례 미적용**(주석 "[XT확정 7/10] 부분소각 무조건 0.01"). OVERCAP_FIXED_LOT·MAX_SIDE_LOTS·GRIND_MIN_BURN_LOT은 전부 base 비례 → 청크 3종만 누락.
  - XGrind_NeedsRescue = [노후 240분 ∨ 사이클 부동 ≤ −DEEP_USD] ∧ hasLoss — **통문 그룹TP까지 남은 거리를 보는 항 전무**("곧 통째로 나갈 판이면 보류" 게이트 부재).
  - 달러 상수(DEEP_USD 1500·PLAN_EPS 1.0·약수익 50/랏)가 센트계좌에선 실화 1/100로 평가.
  - LHEDGE_PlanCompute가 목표가를 최소 이동거리(bestD)로 풀어 터치 순간 1스텝치 잉여만 = 항상 0.01. LHEDGE_PlanAwake는 arm 전 잠김.
- **실측 대조(같은 5시간 창)**: 600029 발사 24·절단 0.01×22+0.02×2 / 33753512 발사 35·절단 **0.01×35(예외 0)**. 33753512 결정적: 깊은 셀 북(0.82랏)이 통문 TP 4062.28 정상 대기($11.6만 내려오면 전량 청산)인데, EA는 위쪽 신생 바이를 0.01씩 35회 상호소각(깊은 셀 대상 절단 0건). 얕은 셀 절단은 셀 BEP를 내려 통문 TP를 더 멀게 함 = 자기 출구 훼손.

## ★ 핵심 재정의 — "이슈 아니면 통째" (사장님 7/31)
- 사장님: "이슈 상황 아니면 부분 청산이 아니라 포지션 전체를 날려야 한다. 날리고 재진입 자리 오면 시간 딜레이 없이 재진입 — 이게 셀프 그라인딩 개념 아니냐, 어디서 망가졌냐".
- 판정: **셀프 그라인딩(Grind_Try) 본체는 무결**(PositionClosePartial 0건, 통째만). 33753512가 정상 구동 증명(각 층 5시간에 8~10회 [통째 청산→45초~3분 뒤 재진입]).
- 개념 훼손 지점 = **"통째를 못 덮으면 덮는 만큼만 깎는다" 예외가 5회에 걸쳐 별도 기계로 증식**: ①7/10 XT 0.01 ②7/11 L8+ 0.02 ③OVERCAP-D 0.05 ④7/13 v5.3.3 #8 Fire 꼬리·GrindBurn ⑤7/22 EXITPLAN 1스텝. lessons L6(우선순위 역전) 재발.
- 🔴 최대 발견: **LHEDGE_Fire 배선이 의도와 정반대** — `if(g_lh.active && RatioFloor()>0.005) return;` = 위기 국면엔 전량 소각 보류하고 부분 절단(EXITPLAN)에 넘김. 사장님 조문은 "평시=통째 / 위기=부분", 코드는 "평시=부분 허용 / 위기=통째 금지". 단 이 조항은 7/22 방패 박리 처방이라 뒤집을 때 근거 재검 필요.
- 처방 조문안(컨펌 대기): 공통 IsCrisisPhase() 신설 → 비위기면 부분 절단 기계 전부 침묵(못 나가면 깎지 말고 대기), 위기에서만 부분 허용 + 청크 base 비례(P1). 개별 P1~P6(청크 base 비례화·통문 근접 보류 게이트·BEP 악화 금지·PlanCompute req 상향·연료 평가에 스왑 편입·arm 모집단 777 제외).

## ★ 위기 축 스펙 확정 (사장님 7/31)
- **위기 임계 = 단리(base 0.01) 기준 $6,000, 복리는 배율(GetActiveLot()/BaseLot) 비례** — 이 축 하나로 헷징 arm + 통째/부분 분기 동시 지배.
- 파라미터 노출: 배포(locked)는 상수 고정 / internal·TEST는 input.
- **777(해결사) 기본 제외 확정** — 위기·arm 판정에서 해결사 기본 OFF, 포함은 토글("해결사 인식하면 레이어가 쌓인다"; 600029 실측: 777 12.60랏 = 셀 랏 31%·부동 41%, 본대만이면 임계 미달).
- 🔴 반증(중요): 위기 축 $6,000을 **헷지 arm에 적용한 백테스트는 기준선에 패배**(5/5~13 기본 +518·EqDD 35.8% vs $6000 계열 −4,970~−9,716·EqDD 37.7~50%). 패인 = arm 2h43m 지연 + 절반 회복 리셋이 위기 중 arm을 2~4회로 분절. 극한창이라 편익(평시 억제) 미측정 = 보류이지 기각 아님. **권고: 위기 축은 통째/부분 분기에 먼저 적용, 헷지 arm은 현행 랏 1.73 유지**(단 7/31 라이브는 ARM_USD 6000 반영됨 — 04 참조, 미검증 리스크 상태).

## 스킬 미설치 발각 + 교훈 번호 정리 (7/31)
- ⚠ 설치본이 여전히 **goldminer-shd-dev 7/6판**(구 사다리 2.78/×8.3/×10이 "절대 수정 금지"로 잔존, lessons L1~L10만). 7/14 이후 산출물 전부 미설치. → 무손실 스킬 통합에 과거 zip/md 업로드 필요.
- 조문 번호 확정 재배정: L17=7/27판(라이브 법의학) / L18=7/31 별도대화 7조문(out딜 반전·버그 라벨 전 3문·반사실 미래정보 금지·대조군 없는 런 판정 금지·주장 등급 표기·기저율(신규 장치 7전 전패)·복구가능성 3중) / L19~L24=스케일 축·폴백 예산제·정책 단일 관문·출구 비파괴·스케일 대조·빌드 지문. 산출 작업지침_L19-L24_및_위기축_검증콘솔_20260731.md.
- 인계(서버안전 세션 → 그라인딩 세션): 패치 베이스 GoldMiner_v5_5_4_{internal,locked,TEST_A}_20260730.mq5. 7/30 수리 = 청산 경로 2곳 선점(S1 CloseProfitPositions에 GX_FireOK·S2 FC/CLOSE ALL 래치에 GX_FireOK+NoteCloseFail) → 그라인딩 세션 P2·P3·C2~C4가 같은 영역이라 앵커 재확인 필수. GX_FireOK 호출 7(정의1+기존3+신규3).
