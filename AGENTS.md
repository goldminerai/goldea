# 작업 지침

이 저장소는 골드(XAU)와 FX 자동매매용 MT4/MT5 EA 개발 저장소다.

## 우선순위

1. 최신 내부 개발본과 해당 개발 명세를 먼저 대조한다.
2. 규칙 변경이나 로직 변경 전 변경 목적과 영향 범위를 설명한다.
3. MQL4/5 수정은 작은 단위로 하고, 동일 문구가 여러 번 있을 때는 위치를 확인한 뒤 수정한다.
4. 컴파일(F7), 괄호/함수 중복 검사, 백테스트 또는 재현 절차를 기록한다.
5. 원본과 테스트본을 혼동하지 않는다. 잠금/배포본은 기준 소스가 아니다.

## 보안

- 계좌번호, API 키, 웹훅 인증값, 라이선스 서명키는 커밋·push 금지.
- `_local_archive/`는 PC/Claude 원본 보관용이며 GitHub에 올리지 않는다.
- 실제 키가 코드에 하드코딩된 파일은 push 전에 반드시 별도 설정으로 분리하거나 비공개 보관한다.

## 주요 위치

- `src/mt5/`: 현재 작업 기준본
- `docs/claude-import/`: 민감정보를 제외한 Claude 인수 문서
- `_local_archive/`: PC와 Claude 작업공간에서 수집한 원본
