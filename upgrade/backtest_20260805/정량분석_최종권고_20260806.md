# 헷지 계통 정량 분석 — 최종 권고 (2026-08-06)

정량 분석관 3종 병렬(캡·정격·회랑 / 방패 사이징·보호 / 출구 봉쇄 확률). 전부 손계산 근거.

## 뿌리 원인: B2 스윕(7/23)이 상수 간 불변식 2개를 깼다

7/23에 세 상수를 동시에 올렸는데(`MAX_RATIO` 0.50→0.70, `RESCUE_RATIO` 0.50→0.70,
`RATIO_FLOOR_MAX` 0.40→0.50), 상수들 사이에 지켜져야 할 부등식은 검토되지 않았다.

| 불변식 | 현행 | 결과 |
|---|---|---|
| `RESCUE_RATIO ≤ RATIO_NORM` | 0.70 > 0.50 **위반** | 리필당 한계비율 0.682 > 정격 0.50 → 비율 단조 상승 → **리필 2~3회에 재전개 영구 사망**(그때 부동 −8,221~−10,297) |
| `RATIO_FLOOR_MAX < RATIO_NORM` | 0.50 = 0.50 **위반** | 회랑 폭 0 → 비율이 평형점(0.468~0.532)에 앉는 순간 **증축도 절단도 불가한 절대 교착** |

모델 검증: 궤적 모델의 최종 EqDD 예측 $10,034 vs 기록된 5월 B2 실측 $9,999 — **오차 0.3%**.

## 권고 ① 상수 3종 (A/B 백테스트로 즉시 검증 가능, 코드 위험 0)

| 상수 | 현행 | 권고 | 근거 |
|---|---|---|---|
| `LHEDGE_RESCUE_RATIO` | 0.70 | **0.50** | 한계비율 0.682→0.500. 궤적 최대 0.484 < 0.50 → 재전개 **영구 생존**. 3경로 게이트 배선과 수학적 등가 |
| `LHEDGE_RATIO_FLOOR_MAX` | 0.50 | **0.40** | 회랑 폭 0→0.10. 위기3단계 절단여력 0.23→0.48랏(2.1배), 차단시점 0.02→0.24(12배). GX cmin과 자동 통일 |
| `LHEDGE_MAX_RATIO` | 0.70 | **동적** `min(snap×0.70, 캡−반대편그리드−0.31)` (상수 폴백 0.60) | 헷지랏과 반대편 그리드랏은 같은 2.5 예산을 1:1로 나누고 상쇄효과도 1:1 → room-bound에선 순노출 `물린랏−2.5` 고정, **0.70은 방패를 두껍게 하는 게 아니라 202604→111로 얼리는 것**. 대가로 반대편이 L8 도달만으로 차단(0.50 대비 2층 조기, 7/14 교착 계보) |

## 권고 ② 코드 수리 — 회귀 위험 낮은 순

| # | 수리 | 행수 | 효과 |
|---|---|---|---|
| Q1 | `anchorLot = LHEDGE_LotFloor(g_lh.snapLots × 0.0875)` (L11 배지·폴백 삭제) | 1 | 커버리지 **CV 24.2%→1.0%**, 편차밴드 −40%~+13%→±3%. **정상경로 0.15 정확 재현 = 회귀 0**. 손실항등식 `loss=100·T·D`에서 loss가 ARM에 고정 → T가 상태를 완전 결정 → T비례가 달러축의 유일한 구현형 |
| Q2 | 쿨다운을 **발사 성공** 시점으로 이동 (`g_lhPlanLastFire`) | 2 | D=0 스테일 플랜 상시터치 → **돌려깎기 전면 락업** 원천 소멸 |
| Q3 | 전역사유 선인출 + `PlanExecute` 18062 `return`→`continue` | 5 | 라운드당 발사 기대값 **+37~43%**. 같은 루프의 다른 유보 5곳은 이미 `continue` — 조문 위반 복원 |
| Q4 | `PlanPin` `effFloor`에 f_min 0.20 주입 (**`RatioFloor()` 본체 불변**) | 1 | 되돌림 국면 방패 전량박리(1회 35%) 차단. 단조사다리 0.20→0.25→0.375→0.50 보존. 본체 수정 시 4게이트 영구 ON되어 **진짜 고아 청소 불가**가 되므로 금지 |
| Q5 | GX `G0-①`에 상환완결형 예외 (**`G1` 락창 검사는 원판 유지 필수**) | 1 | FIRE·COMBO·RESID 전량출구 통과확률 0~3%→정상. 통과조건이 "헷지쪽 랏 ≥ 2.03×헷지랏"인데 전개게이트 대입 시 "비물린측 > 물린측"이라는 **자기모순** |
| Q6 | 양방향 교대 상태변수 | 5 | UP 점유 67%→50%. BUY물림 캠페인의 방향편향 제거 |
| Q7 | GX DENY 로그 10분 스로틀 | 3 | Q2·Q3의 **전제조건**(continue 전환 시 로그 폭주 방지) |
| Q8 | Fire 꼬리절단 하한에 FIX3 완화 대칭 적용 | 1 | 방패만 팔고 소각 0 방지(배율高 + 깊이$50+ 에서 결정론적 발동) |
| Q9 | C1 침묵해제 시 `g_lhPlanFpCnt=-1` 초기화 | 1 | 침묵 해제 후 돌려깎기 무기한 정지 방지 |
| Q10 | `Grind_BundleTP`에 `LHEDGE_MergeOffset` 반영 | 1 | E1이 밀기 전 gTP에서 통째 발사 → 편입예산 증발 방지 |
| Q11 | 스테일 `mergeTP` 청소 조건에 ListMode 2(셀 전멸) 포함 | 2 | 없어진 예산으로 헷지손실 실현(§0 위반) 방지 |

## 검증 계획
1. **상수 3종 A/B** — 프리셋만 바꿔 즉시 백테스트(5/6~5/22). 4런: 현행 / RESCUE만 / FLOOR만 / 둘다
2. **코드 수리** — Q1·Q8~Q11(저위험) 먼저 → 정적검증 → 컴파일 → 백테스트 → Q2·Q3·Q5(구조) 순차
3. 백테스트 불가 항목(A2·감시견·§16)은 벤티지 데모 라이브 관찰

## 판정 등급 주의
- 7/14 라이브 로그 직접 대조 미실시(추정), 04문서 원문 일부 미확인(수치 정합만 확인)
- "68% 비대칭"은 헷지÷셀 랏 비율이지 UP:DN 발사비율이 아님 — 앞선 감사관 인용 오류 정정됨
